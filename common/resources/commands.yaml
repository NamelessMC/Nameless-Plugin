# The server needs to be fully restarted for changes to take effect, commands cannot easily be dynamically registered
# on most platforms and would break for Minecraft 1.13+ anyway.

# Commands can be disabled by commenting a line.

get-notifications: 'notifications'
register: 'register'
report: 'report'
user-info: 'webinfo'
verify: 'verify'
plugin: 'nlpl'
